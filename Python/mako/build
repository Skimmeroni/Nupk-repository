#!/bin/sh -e

gzip -d Mako-$VERSION.tar.gz | tar -x
cd Mako-$VERSION

python setup.py build
python setup.py install \
	--prefix=/usr \
	--root="$DESTDIR"

find "$DESTDIR/usr/lib" -type f -name '*.so' -exec strip --strip-unneeded {} \;
